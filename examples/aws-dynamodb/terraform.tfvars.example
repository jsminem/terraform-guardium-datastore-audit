# AWS DynamoDB with Universal Connector - Terraform Variables

#----------------------------------------
# General Configuration
#----------------------------------------
aws_region  = "us-east-1"
aws_partition = "aws"
name_prefix = "dynamodb-gdp"

tags = {
  Environment = "Demo"
  Owner       = "Security"
  Project     = "Guardium-Terraform"
}

#----------------------------------------
# DynamoDB Configuration
#----------------------------------------
# Use "all" to monitor all tables, or provide a comma-separated list of table names
dynamodb_tables = "all"

# If you want to use existing CloudTrail or CloudWatch Log Group, uncomment and set these
# existing_cloudtrail_name = "your-existing-cloudtrail"
# existing_cloudwatch_log_group_name = "/aws/cloudtrail/your-existing-log-group"

#----------------------------------------
# Universal Connector Configuration
#----------------------------------------
enable_universal_connector = true
udc_aws_credential = "guardium-aws"  # Name of the AWS credential stored in Guardium Central Manager

# CSV Configuration
csv_start_position = "end"
csv_interval = "5"
csv_event_filter = ""
csv_description = "DynamoDB Universal Connector"
csv_cluster_name = ""

# CSV Profile Directory Configuration
#----------------------------------------
# These paths are used for uploading CSV profile files to Guardium
#
# DEFAULT (CLI user with chroot - RECOMMENDED):
#   profile_upload_directory = "/upload"  # Chroot path for CLI user SFTP
#   profile_api_directory = "/var/IBM/Guardium/file-server/upload"  # Full path for API
#
# The defaults work with CLI user + SFTP chroot configuration
# See: https://github.com/IBM/terraform-guardium-gdp/blob/main/docs/preparing-guardium.md
#----------------------------------------
# profile_upload_directory = "/upload"  # Uncomment to override default
# profile_api_directory = "/var/IBM/Guardium/file-server/upload"  # Uncomment to override default

#----------------------------------------
# Guardium Connection Details
#----------------------------------------
gdp_server             = "guardium.example.com"  # Replace with your Guardium server
gdp_port               = 8443
gdp_username           = "admin"                 # Replace with your Guardium username
gdp_password           = "password"              # Replace with your Guardium password
gdp_client_id          = "client4"               # Replace with your client ID
gdp_client_secret      = "client-secret"         # Replace with your client secret
gdp_ssh_username       = "cli"                   # Use "cli" for secure CLI user (recommended) or "root"
gdp_ssh_privatekeypath = "~/.ssh/id_rsa"         # Replace with your SSH private key path
gdp_mu_host            = ""                      # Comma separated list of Guardium Managed Units
